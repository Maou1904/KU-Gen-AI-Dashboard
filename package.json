{
  "name": "kucsgenai-dashboard",
  "version": "1.0.0",
  "description": "KU Gen-AI Dashboard frontend and backend workspace",
  "scripts": {
    "start": "npm run frontend",
    "frontend": "http-server frontend -p 8080 -c-1",
    "backend": "npm --prefix backend start",
    "backend:dev": "npm --prefix backend run dev",
    "install:all": "npm install && npm --prefix backend install"
  },
  "dependencies": {},
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
