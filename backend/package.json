{
  "name": "kucsgenai-dashboard-backend",
  "version": "1.0.0",
  "description": "KU Gen-AI Dashboard Backend API Server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "check": "node --check server.js && node --check config/database.js && node --check services/sync-service.js && node --check services/scheduler.js && node --check routes/sync.js",
    "migrate": "node database/migrate.js",
    "sync:once": "node scripts/sync-once.js",
    "test:integration": "node tests/integration.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "kucsgenai-dashboard": "file:..",
    "mysql2": "^3.6.5",
    "pg": "^8.22.0",
    "sequelize": "^6.35.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
